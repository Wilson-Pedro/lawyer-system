export type Entity = {
  id: number;
  nome: string;
};

export type Assistido = Entity & {};

export type Advogado = Entity & {};

export type Estagiario = Entity & {};

export type Professor = Entity & {};
